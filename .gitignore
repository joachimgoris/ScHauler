# Build output
bin/
obj/

# Local SQLite data (disposable per game session; note: git ignore matching is
# case-insensitive on macOS, so a bare data/ would swallow src/Data/ — scope it)
src/Data/hauler.db*
/data/

# OS / editor
.DS_Store

# Design bundles stay local (decision 2026-09-07)
design_handoff_cargo_hold/
*.zip
